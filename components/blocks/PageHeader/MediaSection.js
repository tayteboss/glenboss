import styled from 'styled-components';

const MediaSectionWrapper = styled.div``;

const MediaSection = () => {
	return (
		<MediaSectionWrapper>
			MediaSection
		</MediaSectionWrapper>
	);
};

export default MediaSection;
